# 02 — 前端样板开发流程

> Vue3 通用组件 + request 请求封装调用规范

---

## 一、前置依赖

| 依赖项   | 版本/说明                                                       |
| -------- | --------------------------------------------------------------- |
| Node.js  | ≥ 18.x                                                          |
| 包管理器 | npm / pnpm / yarn                                               |
| 脚手架   | Vite 5 + Vue 3                                                  |
| 核心库   | `pinia`（状态管理）、`vue-router`（路由）、`axios`（HTTP 请求） |
| 后端服务 | `TemplateDormApi` 已启动，监听 `http://localhost:5000`          |
| API 契约 | Apifox 已录入接口 URL 和响应格式                                |

---

## 二、分步操作

### 2.1 创建 Vite + Vue3 项目

```bash
npm create vite@latest template-frontend -- --template vue
cd template-frontend
npm install
```

### 2.2 安装核心依赖

```bash
npm install pinia vue-router axios
```

### 2.3 目录结构

```
template-frontend/
├── src/
│   ├── api/              // 接口请求层
│   ├── components/       // 通用组件（CrudTable、SearchForm、StatusTag）
│   ├── mock/             // 可通过环境变量启用的本地 Mock 适配器
│   ├── router/           // 路由配置
│   ├── store/            // Pinia 状态仓库
│   ├── utils/            // 工具函数（request.js 请求封装）
│   └── views/            // 页面组件
├── .env.local            // 本地环境变量，不提交
└── vite.config.js
```

### 2.4 配置代理（vite.config.js）

```js
export default defineConfig({
  plugins: [vue()],
  server: {
    port: 3000,
    proxy: {
      "/api": {
        target: "http://localhost:5000",
        changeOrigin: true,
      },
    },
  },
});
```

---

## 三、代码模板

### 3.0 开启本地 Mock 登录

后端 JWT 接口未就绪时，在 `.env.local` 中配置：

```bash
VITE_USE_MOCK=true
```

登录页仍统一调用 `authApi.login()`，Mock 只在请求适配层接管 `POST /api/auth/login`。关闭开关后请求会直接进入真实后端，不需要修改页面代码。

临时请求体：

```json
{
  "loginName": "student001",
  "password": "123456"
}
```

临时响应仍遵循统一结构：

```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "token": "mock-token",
    "userInfo": {
      "id": "student001",
      "name": "测试学生",
      "role": "student"
    }
  }
}
```

### 3.1 请求封装 `src/utils/request.js`

<!-- prettier-ignore -->
```js
import axios from 'axios'
import { attachAuthMock } from '@/mock/auth'
import { clearSession } from '@/store/session'
import { useUserStore } from '@/store/user'

const request = axios.create({
  baseURL: '/api',
  timeout: 10000
})

const AUTH_TOKEN_SNAPSHOT = 'authTokenSnapshot'
let loginRedirectPending = false

const redirectToLogin = async (requestToken) => {
  const userStore = useUserStore()
  if (requestToken !== userStore.token) return

  await clearSession()
  if (window.location.pathname === '/login' || loginRedirectPending) return

  loginRedirectPending = true
  const currentPath = `${window.location.pathname}${window.location.search}${window.location.hash}`
  window.location.assign(`/login?redirect=${encodeURIComponent(currentPath)}`)
}

// 请求拦截器：记录请求使用的 Token、携带 JWT，并按开关接入登录 Mock。
request.interceptors.request.use((config) => {
  const userStore = useUserStore()
  config[AUTH_TOKEN_SNAPSHOT] = userStore.token
  if (userStore.token) {
    config.headers.Authorization = `Bearer ${userStore.token}`
  }
  return attachAuthMock(config)
})

// 业务 401 与 HTTP 401 都必须走同一个会话清理入口。
request.interceptors.response.use(
  async (response) => {
    const { code, message, data } = response.data
    if (code === 200 || code === 201) return data
    if (code === 401) {
      await redirectToLogin(response.config[AUTH_TOKEN_SNAPSHOT])
    }
    return Promise.reject(new Error(message || '请求失败'))
  },
  async (error) => {
    if (error.response?.status === 401) {
      await redirectToLogin(error.config?.[AUTH_TOKEN_SNAPSHOT])
    }
    return Promise.reject(error)
  }
)

export default request
```

### 3.2 API 接口封装 `src/api/building.js`

```js
import request from "@/utils/request";

export const buildingApi = {
  getList: (params) => request.get("/building", { params }),
  getById: (id) => request.get(`/building/${id}`),
  create: (data) => request.post("/building", data),
  update: (id, data) => request.put(`/building/${id}`, data),
  delete: (id) => request.delete(`/building/${id}`),
};
```

### 3.3 Pinia 用户仓库 `src/store/user.js`

<!-- prettier-ignore -->
```js
import { defineStore } from 'pinia'

const TOKEN_KEY = 'token'
const USER_INFO_KEY = 'userInfo'

const getLocalStorage = () => {
  try {
    return window.localStorage
  } catch {
    return null
  }
}

const safeGetItem = (key) => {
  try {
    return getLocalStorage()?.getItem(key) ?? null
  } catch {
    return null
  }
}

const safeSetItem = (key, value) => {
  try {
    const storage = getLocalStorage()
    if (!storage) return false
    storage.setItem(key, value)
    return true
  } catch {
    return false
  }
}

const safeRemoveItem = (key) => {
  try {
    getLocalStorage()?.removeItem(key)
  } catch {
    // 存储不可用时退化为 Pinia 内存会话。
  }
}

const clearStoredSession = () => {
  safeRemoveItem(TOKEN_KEY)
  safeRemoveItem(USER_INFO_KEY)
}

const isValidUserInfo = (userInfo) =>
  Boolean(
    userInfo &&
    typeof userInfo === 'object' &&
    !Array.isArray(userInfo) &&
    ['id', 'name', 'role'].every(
      (key) => typeof userInfo[key] === 'string' && userInfo[key].trim()
    )
  )

const readStoredSession = () => {
  const token = safeGetItem(TOKEN_KEY)
  const storedUserInfo = safeGetItem(USER_INFO_KEY)

  try {
    const userInfo = JSON.parse(storedUserInfo)
    if (!token || !isValidUserInfo(userInfo))
      throw new TypeError('登录缓存无效')
    return { token, userInfo }
  } catch {
    clearStoredSession()
    return { token: '', userInfo: null }
  }
}

export const useUserStore = defineStore('user', {
  state: () => readStoredSession(),
  getters: {
    isLoggedIn: (state) => Boolean(state.token && state.userInfo)
  },
  actions: {
    setSession({ token, userInfo }) {
      if (
        typeof token !== 'string' ||
        !token.trim() ||
        !isValidUserInfo(userInfo)
      ) {
        throw new Error('登录响应缺少 Token 或用户信息')
      }

      this.token = token
      this.userInfo = userInfo

      const tokenStored = safeSetItem(TOKEN_KEY, token)
      const userInfoStored = safeSetItem(
        USER_INFO_KEY,
        JSON.stringify(userInfo)
      )
      if (!tokenStored || !userInfoStored) clearStoredSession()
    },
    logout() {
      this.token = ''
      this.userInfo = null
      clearStoredSession()
    }
  }
})
```

### 3.4 通知 API 与 Pinia 仓库

`src/api/notification.js` 与后端 `NotificationController` 保持一致：

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| `GET` | `/notifications` | 按 `page` / `pageSize` / `isRead` 查询通知 |
| `PUT` | `/notifications/{notificationId}/read` | 标记单条已读 |
| `POST` | `/notifications/read-batch` | 传入 `notificationIds` 批量标记已读 |
| `GET` | `/notifications/unread-count` | 查询未读数量 |

`src/store/notice.js` 统一管理通知列表、分页、已读筛选、未读数和加载/错误状态。页面只需调用 action，不应自行重复请求逻辑。

<!-- prettier-ignore -->
```js
import { useNoticeStore } from '@/store/notice'

const noticeStore = useNoticeStore()

await Promise.all([
  noticeStore.fetchNotices({ page: 1, pageSize: 10, isRead: '未读' }),
  noticeStore.fetchUnreadCount()
])

await noticeStore.markRead(notificationId)
await noticeStore.markBatchRead(selectedNotificationIds)
```

`isRead` 只允许 `'已读'`、`'未读'` 或空字符串；`pageSize` 最大为 `100`，与后端校验保持一致。
单条与批量已读操作会通过 `markingIds` 去重，页面可使用 `isMarking(notificationId)` 禁用重复操作。
`clearSession()` 会在手动退出和 `401` 失效时同时清理 user / notice store，避免切换账号后残留通知。

### 3.5 通用 CrudTable 组件 `src/components/CrudTable.vue`

`CrudTable` 是受控分页组件：页面持有查询和弹窗业务状态，组件负责列渲染、空态/加载态、操作入口与分页事件。

| 类型 | 名称 | 说明 |
| --- | --- | --- |
| prop | `columns` | `{ prop, label, width? }[]` 列定义 |
| prop | `data` / `total` / `loading` | 表格数据和请求状态 |
| prop | `page` / `pageSize` | 由页面控制的分页参数 |
| prop | `rowKey` | 行唯一键字段名或函数 |
| event | `create` / `edit` / `delete` | 增删改操作入口 |
| event | `page-change` | 返回 `{ page, pageSize }` |
| slot | `cell-{prop}` / `actions` / `toolbar` | 自定义单元格、行操作和工具栏 |

<!-- prettier-ignore -->
```vue
<CrudTable
  :columns="columns"
  :data="list"
  :total="total"
  :loading="loading"
  :page="currentPage"
  row-key="buildingId"
  caption="楼栋列表"
  @create="openCreateModal"
  @edit="openEditModal"
  @delete="handleDelete"
  @page-change="onPageChange"
/>
```

业务页必须传入稳定的 `rowKey`，避免分页或数据更新后复用错误的 DOM 行。
删除数据导致总页数缩小时，持有分页状态的业务页应将 `currentPage` 回退到最后一个有效页并重新查询。

### 3.6 路由配置 `src/router/index.js`

<!-- prettier-ignore -->
```js
import { createRouter, createWebHistory } from 'vue-router'
import { getSafeAuthRedirect } from '@/router/authRedirect'
import { useUserStore } from '@/store/user'

const routes = [
  {
    path: '/login',
    name: 'Login',
    component: () => import('@/views/LoginView.vue'),
    meta: { public: true, layout: 'auth' }
  },
  {
    path: '/building',
    name: 'Building',
    component: () => import('@/views/BuildingList.vue'),
    meta: { requiresAuth: true }
  }
]

const router = createRouter({
  history: createWebHistory(),
  routes
})

router.beforeEach((to) => {
  const userStore = useUserStore()
  if (to.meta.requiresAuth && !userStore.isLoggedIn) {
    return { name: 'Login', query: { redirect: to.fullPath } }
  }
  if (to.name === 'Login' && userStore.isLoggedIn) {
    return getSafeAuthRedirect(to.query.redirect)
  }
  return true
})

export default router
```

`getSafeAuthRedirect()` 只允许跳回已登记的站内业务路由，并拒绝跨域地址、反斜杠及登录页循环目标。

---

## 四、验收标准

| 检查项    | 标准                                                       |
| --------- | ---------------------------------------------------------- |
| 项目启动  | `npm run dev` 无报错，浏览器可访问 `http://localhost:3000` |
| 登录页面  | 未登录访问业务页面时自动跳转 `/login`                      |
| Mock 登录 | 正确账号可登录，错误账号显示提示，页面不直接依赖 Mock 实现 |
| 状态恢复  | 刷新页面后 Token 和最小用户信息仍可恢复                    |
| 退出登录  | 清理本地状态并返回登录页                                   |
| 通知仓库  | 列表、已读筛选、单条/批量已读与未读数均可调用       |
| 代理转发  | 前端 `/api/*` 请求正确转发到后端 `localhost:5000`          |
| 列表展示  | 楼栋数据分页展示，列字段与后端 DTO 对齐                    |
| 新增功能  | 新增弹窗提交后，列表自动刷新                               |
| 编辑功能  | 回显正确，修改可保存                                       |
| 删除确认  | 删除前弹窗确认，删除后列表刷新                             |
| 统一组件  | CrudTable、SearchForm 组件正确封装，其他页面可直接复用     |

---

## 五、常见报错

| 报错信息                                | 原因                   | 解决方案                                                 |
| --------------------------------------- | ---------------------- | -------------------------------------------------------- |
| `Network Error`                         | 后端未启动或端口不对   | 确认后端监听 `5000` 端口，检查 `vite.config.js` 代理配置 |
| `401 Unauthorized`                      | Token 过期或缺失       | 检查 `localStorage` 中 `token` 是否存在                  |
| `Cannot read properties of undefined`   | API 返回数据格式不匹配 | 确认后端返回 `{ code, message, data }` 格式              |
| `AxiosError: timeout`                   | 请求超时               | 检查数据库连接，确认 Oracle 容器正常运行                 |
| `Failed to resolve component CrudTable` | 组件未注册             | 在 `main.js` 中全局注册或在页面中局部引入                |
| `Module not found: @/utils/request`     | 路径别名未配置         | 在 `vite.config.js` 中配置 `resolve.alias`               |
