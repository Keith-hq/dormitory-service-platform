# 难点⑤ SLA 派单 — 数据库影响说明（DDL 变更清单）

> 四审更新（2026-08-13）：难点⑤ 的 DDL 已由数据库负责人确认为编号迁移
> [021_sla_dispatch.sql](../../database/ddl/extensions/021_sla_dispatch.sql)（PR #51，已合入 develop，
> merge bf6c72b）落地：[sp_sla_dispatch.sql](../../database/sp/sp_sla_dispatch.sql) 不再内联任何 DDL。
> 本文件 §0/§1 的"待确认事项"与"迁移编号提案"已按裁决更新。

## 0. 待确认事项（数据库负责人通道）

| # | 事项 | 状态 | 说明 |
|---|------|------|------|
| 1 | 4 处 DDL（2 增列 + 2 CHECK）落地形式 | ✅ **已裁决** | 编号迁移 [021_sla_dispatch.sql](../../database/ddl/extensions/021_sla_dispatch.sql)（PR #51，已合入 develop）：Part A 增列 Escalation_Time、Part B 增列 Repair_Result VARCHAR2(200 CHAR) + SEQ_SLA_LOG、Part C 两个 CHECK |
| 2 | 审计流水 `SEQ_SLA_AUDIT` 是否需要 | 待确认 | 三审后 SP 已**不再写** `D_Audit_Event`（MAX+1 并发撞主键，见 §4）；如需恢复审计，先确认 `D_Audit_Event` 数据拥有方与序列命名，再补编号迁移 |
| 3 | 约束校验存量数据 | ✅ **已裁决** | 迁移 021 Part C：`CK_D_ADMIN_ROLE`（`Role_Level IN ('楼长','维修员','超级管理员')`）；`CK_D_REPAIR_TICKET_STATUS`（`Status IN ('待处理','已派单','已完成','已撤销')`，4 值状态机）。执行前须跑存量合规检查，存量'处理中'归并到'已派单'（021 文件内已附语句） |
| 4 | 通知端到端投递依赖通知中心模块主键机制 | 待复验 | 现网 `D_Notification` 无触发器/默认值（`SEQ_NOTIFICATION` 存在但无 `TRG_*`），develop 基线上通知仓储插入报 ORA-01400；该问题由难点④ PR #36（迁移 `012_notification_id_sequence.sql` + 仓储 `NEXTVAL` 预取）修复，合入后对 `POST /api/internal/scheduler/sla-escalation` 做一次端到端复验（见 §7.4） |

确认渠道：数据库负责人（建议在评审裁决表 / 项目群同步），确认后更新本文件状态为"已确认"。

## 1. 迁移落地（已裁决）

数据库负责人已确认并将本难点 DDL 合并为编号迁移 **`database/ddl/extensions/021_sla_dispatch.sql`**
（PR #51，已合入 develop，merge bf6c72b；2026-08-13 定稿，C-033/C-034）：

| 段 | 内容 |
|----|------|
| Part A | `D_Repair_Ticket` 增列 `Escalation_Time DATE` |
| Part B | `D_Repair_Log` 增列 `Repair_Result VARCHAR2(200 CHAR)` + 序列 `SEQ_SLA_LOG`（MAX(Log_ID)+1 起点，NUMBER(10) 上限防护，幂等） |
| Part C | `CK_D_REPAIR_TICKET_STATUS`（4 值状态机）+ `CK_D_ADMIN_ROLE`（⚠️ 依赖状态机改造完成，含存量合规检查与'处理中'归并语句） |
| 兼容对齐 | 已执行过内联补丁的环境：`ALTER TABLE D_Repair_Log MODIFY (Repair_Result VARCHAR2(200 CHAR))`（BYTE→CHAR） |

执行顺序：全量重建链 010→…→020→021；增量环境 Part A/B 可先行，Part C 须在 PR #45
状态机改造完成后执行。迁移前由数据库负责人对现有环境执行 §3 的 verify 语句并留存结果。

## 2. 变更明细

### 2.1 `D_Repair_Ticket.Escalation_Time DATE`

- **语义**：`NULL` = 从未升级；`NOT NULL` = 已升级（防二次升级的唯一事实来源）。
- **写入方**：`SP_Escalate_SLA`（条件 UPDATE `WHERE Escalation_Time IS NULL` + `SQL%ROWCOUNT` 守门，多实例并发安全）。
- **读取方**：DORM-26 待处理列表（DTO `EscalationTime` 字段，仅展示）。
- **受影响接口**：`GET /api/admins/{adminId}/repair-tickets`（DORM-26）、`POST /api/internal/scheduler/sla-escalation`（SVC-SCHED-03）、Quartz `SlaEscalationJob`（每 15 分钟）。
- **回滚**：`ALTER TABLE D_Repair_Ticket DROP COLUMN Escalation_Time;`（需先停用 SLA 升级巡检）。

### 2.2 `D_Repair_Log.Repair_Result VARCHAR2(200)`

- **语义**：维修结果枚举（契约 DORM-28）：`已修复` / `需更换配件` / `无法修复`；可空（历史数据/未提供时 NULL）。
- **写入方**：`SP_Complete_Repair` 参数 `p_Repair_Result`（C# 侧已做枚举校验）。
- **受影响接口**：`POST /api/repair-tickets/{ticketId}/logs`（DORM-28，请求字段 `repairResult`）。
- **回滚**：`ALTER TABLE D_Repair_Log DROP COLUMN Repair_Result;`

### 2.3 `CK_D_REPAIR_TICKET_STATUS`

- **定义**（迁移 021 Part C，四审 4 值状态机）：`CHECK (Status IN ('待处理','已派单','已完成','已撤销'))`。
- **影响**：`SP_Assign_Ticket`（待处理→已派单）、`SP_Complete_Repair`（已派单→已完成）写入路径的兜底校验；
  `SP_Claim_Ticket` 不再写 Status（开工与否以维修日志是否存在判定，PR #44 裁决 §四-3）；
  非法状态写入将被拒绝（ORA-02290）。
- **执行前提**：状态机改造完成 + 存量合规检查（'处理中'归并到'已派单'），见迁移 021 文件头与 Part C 说明。

### 2.4 `CK_D_ADMIN_ROLE`

- **定义**（迁移 021 Part C）：`CHECK (Role_Level IN ('楼长','维修员','超级管理员'))`。
- **影响**：`SP_Assign_Ticket`/`SP_Escalate_SLA` 按 `Role_Level` 查询维修员/楼长，约束保证角色域一致；与 `AuthPolicies` 的 role claim 映射（admin→楼长、repairman→维修员、super_admin→超级管理员）配套。

## 3. Verify 语句（部署后核对）

```sql
-- 列存在性
SELECT COLUMN_NAME, DATA_TYPE, DATA_LENGTH, NULLABLE
FROM   ALL_TAB_COLUMNS
WHERE  OWNER = 'DORM_OPER'
  AND  (TABLE_NAME = 'D_REPAIR_TICKET' AND COLUMN_NAME = 'ESCALATION_TIME'
     OR TABLE_NAME = 'D_REPAIR_LOG'    AND COLUMN_NAME = 'REPAIR_RESULT');

-- 约束存在性（注意 CONSTRAINT_TYPE = 'C'）
SELECT TABLE_NAME, CONSTRAINT_NAME, SEARCH_CONDITION
FROM   ALL_CONSTRAINTS
WHERE  OWNER = 'DORM_OPER'
  AND  CONSTRAINT_NAME IN ('CK_D_REPAIR_TICKET_STATUS', 'CK_D_ADMIN_ROLE');

-- 存量数据合规性（加约束前必查，须返回 0 行）
SELECT DISTINCT Role_Level FROM D_Admin
WHERE  Role_Level NOT IN ('楼长','维修员','超级管理员');
SELECT DISTINCT Status FROM D_Repair_Ticket
WHERE  Status NOT IN ('待处理','已派单','已完成','已撤销');
```

## 4. 三审相关变更说明（SP 行为变化）

- **不再写 `D_Notification`**：原 `MAX(Notification_ID)+1` 双实例并发会撞主键，且通知数据属于通知中心模块（拥有者边界）。现由 `SP_Escalate_SLA` 通过 `SYS_REFCURSOR` 返回本次新升级工单清单，应用层 `SlaDispatchService` 调用公共服务 `INotificationService.CreateAsync` 投递（其内部使用通知模块自己的主键机制）。
- **不再写 `D_Audit_Event`**：同理，`MAX(Audit_ID)+1` 并发撞主键；审计序列方案见 §0 待确认事项 #2。升级痕迹可由 `Escalation_Time` + 通知记录推导。
- **并发模型**：`FOR UPDATE SKIP LOCKED`（按 `Ticket_ID` 排序固定加锁顺序，避免双实例交叉死锁）+ 条件 UPDATE + `SQL%ROWCOUNT` 检查——每个工单恰好被一个实例升级/通知一次。双会话竞态验证脚本见 [test_escalation_race_*.sql](../../database/sp/test_escalation_race_setup.sql)。
- **通知投递失败不升级为 500**：升级标记已由 SP 原子提交，端点若因通知失败返回 500，调度方重试也扫不到该工单（`Escalation_Time` 已非空）→ 提醒永久丢失。因此 `SlaDispatchService` 对每条通知投递失败记录 `LogError`（含工单号/房间/楼长），端点仍返回本次升级清单；develop 基线上的 ORA-01400 属通知模块缺口（见 §0 #4），难点④ 合入后投递即通。

## 5. 受影响接口清单（汇总）

| 接口/组件 | 编号 | 影响 |
|-----------|------|------|
| `GET /api/admins/{adminId}/repair-tickets` | DORM-26 | 读取 `Escalation_Time`（列表字段） |
| `POST /api/repair-tickets/{ticketId}/logs` | DORM-28 | 写入 `Repair_Result`（字段 `repairResult` + 枚举校验） |
| `POST /api/internal/scheduler/sla-escalation` | SVC-SCHED-03 | 触发升级标记（列 + 并发守卫） |
| `POST /api/internal/scheduler/assign-ticket` | 文档化扩展 | 写入受 `CK_D_REPAIR_TICKET_STATUS` 约束域 |
| Quartz `SlaEscalationJob` | — | 每 15 分钟巡检，多实例安全 |
| `SP_Assign_Ticket` / `SP_Claim_Ticket` / `SP_Complete_Repair` | — | 受两个 CHECK 约束域影响 |

## 6. 状态

- [x] 三审修复完成（内嵌幂等补丁 + 异常码修正 + 并发守卫 + 边界对齐）
- [x] 四审修复完成（4 值状态机 + 内联 DDL 移除，由迁移 021 提供）
- [x] Oracle 实跑验证完成（证据见 §7：SQL 套件 14/14、双会话竞态、HTTP 全链路、边界与清理）
- [x] 数据库负责人确认（§0 #1/#3 已裁决 → 迁移 021；#2 审计序列待确认、#4 通知端到端待复验）
- [x] 迁移文件落地（021_sla_dispatch.sql，PR #51 已合入 develop）与 verify 留存
- [ ] 难点④ 合入后端到端通知投递复验（§0 #4）

## 7. Oracle 实跑验证证据（2026-08-13，Docker Oracle XE 21c @ DORMDB）

### 7.1 SQL 测试套件（14/14 PASS）

`test_sp_sla_dispatch.sql` 三审修复版全量通过：SP 编译 4/4（Assign/Claim/Complete/Escalate），
含 DORM-26 分页 SQL 形态（COUNT 总数 + 分页切片 + OFFSET 边界）、游标升级清单（首扫含票、二扫为空）、
越权/未接单/重复完工拒绝、清理只删自有数据（按 `t5_` 前缀账号 + 已知 Ticket_ID 范围）。

### 7.2 双会话竞态（多实例等价）

两 sqlplus 会话并行执行 `SP_Escalate_SLA`（3 张超期工单）：
- 会话 1：`ESCALATED_TICKET=2,3,4`，`TOTAL_ESCALATED=3`
- 会话 2：`TOTAL_ESCALATED=0`（SKIP LOCKED 全部跳过，无 ORA 异常）
- 校验：`UNESCALATED_RACE_TICKETS=0`（每张恰好升级一次）、`RACE_VERIFY_OK`、`RACE_CLEANUP_OK`

### 7.3 HTTP 全链路（真实 JWT：学生/维修员；后端 localhost:5000）

| 用例 | 结果 |
|------|------|
| 学生 token 访问 DORM-26 | HTTP 403（策略拒绝） |
| 维修员 DORM-26 page=1&pageSize=1 | 200，`{"items":[{ticketId:1...}],"total":3,"page":1,"pageSize":1}`（真实分页下推） |
| 访问他人 adminId 的 DORM-26 | 200 体 `code:403 "无权访问其他管理员的工单"` |
| 旧路由 `escalate-sla` | HTTP 404（契约路由已切换） |
| `sla-escalation` 无 X-Service-Key | HTTP 401 |
| `POST /api/internal/scheduler/sla-escalation` | 200 `SLA升级巡检完成`；超期工单 `Escalation_Time` 落库；SP 未写任何 `D_Notification`/`D_Audit_Event`（边界证据：`NOTIF_ROWS=0`、`AUDIT=0`）；通知投递失败由服务记 `LogError`（工单#4，ORA-01400 来自通知仓储，见 §0 #4） |
| DORM-27 接单（指派给本人） | 200 `接单成功` |
| DORM-28 `repairResult:"fixed"`（非法枚举） | 200 体 `code:400 "维修结果必须为：已修复/需更换配件/无法修复"`（未触库） |
| DORM-28 `repairResult:"已修复"` | 200 `维修完成`；`D_Repair_Ticket.Status=已完成`、`D_Repair_Log.Repair_Result=已修复`、`Resolve_Time` 落库 |

### 7.4 验证环境清理与遗留说明

- 全部 HTTP 夹具按自有数据标记（`Issue_Desc LIKE 'HTTP:%'` + `t5h_*` 账号/`T5H_*` 管理员）删除：
  `logs=1 tickets=3 accts=2 admins=2`，提交前无残留。
- 真实工单 #1（灯泡坏了）曾由 assign 端点验证指派给临时维修员 T5H_REP，已恢复为
  `Assigned_To=NULL`（测试前状态，交回 `SP_Assign_Ticket` 正式派单）。
- 通知端到端投递需待难点④ PR #36 合入后复验（develop 基线通知仓储 ORA-01400，见 §0 #4）；
  本 PR 无任何通知模块文件改动，边界由 `SP_Escalate_SLA` 零写 + 服务层只调公共接口保证。
