# 集成测试用例 · 认领文档：辛泓毅（报修·个人中心·晚归·安全巡检）

> **版本**：拆分自《集成测试用例-v1》v1.5 ｜ **拆分日期**：2026-08-16 ｜ **维护人**：李泽远（PM）
> **你的认领范围**：主执行 **3 条用例**（P0×0 / P1×1 / P2×2），另 **3 条**联调协办。对应链路：**C4-004 撤销工单与报修图片、C9 巡检/保洁/晚归**。
> **归属依据**：报修学生端（STU-08~12）+ 个人中心（STU-01/02/03/18）+ 晚归（STU-13/14、DORM-31）+ 卫生巡检（DORM-32~34）+ 门禁访客核验（ACCESS-01/02、VST-01~03）+ 违规登记（VIOL-01/02）归你（《接口认领》§2.6）；复核申诉（APPEAL-03）已移交兰皓衍，你不再负责。
> **契约事实来源**：`默认模块.openapi.json`（Apifox 导出，冻结不可改）。

---

## ⚠️ 给你的使用说明（先读）

1. **认领执行**：第一节每条用例，把 `执行:` 填上你的名字；逐条核对「通过判定 / DB 断言 / 前置与测试数据」是否与你的实现一致——**不一致当场改**。
2. **数据样本**：你的 3 条主执行用例全部在 C6–C11 组，**PM 未锁数据样本，需你按 Apifox 契约补**（已标注）。补完你的任务才算闭环。
3. **你是报修学生端负责人**：李昂的派单链路（C4-001）依赖你的 STU-08/09，执行前先对齐。
4. **执行时**：照用例跑，`通过判定` 全部满足才打「通过」；失败记 `失败+缺陷号`，缺陷进 bug 清单（字段见第五节）。

---

## 0. 契约口径（判定标准的地基）

| 项 | 约定 |
|---|---|
| 统一响应 | `{ code, message, data }`；**code=0/200 成功**，非 0 为业务错误码 |
| 分页 | 参数 `page/pageSize`，返回 `{ items, total, page, pageSize }` |
| 鉴权 | 登录 `POST /auth/login` 拿 token，后续接口带 `Authorization: Bearer <token>` |
| 幂等 | **`Idempotency-Key` 请求头**（契约显式标注）：STU-05 缴费、STU-07 充值、STU-25 借用、DORM-29 耗材出库 |
| data 未展开 | 多数 POST 的 `data` 契约未定义 → **成功以 code=0/200 判定，业务结果用后续 GET + DB 断言验证** |

**测试数据预留**（同一开发库，避免撞车）：

| 资源 | 预留值 |
|---|---|
| 学生 studentId | IT_STU_001、IT_STU_002… |
| 房间 roomId | 900101…900204 |
| 报修工单 ticketId | 按 Apifox 契约补（一条待派单工单用于撤单 004） |

---

## 1. 你主执行的用例

### 4.4 C4 报修 SLA 派单（P1 · 李昂×你）— 你主执行撤单/图片侧

> 数据样本需你按 Apifox 契约补；报修→派单主链路（001）由李昂主跑、你协办学生端。

#### IT-C4-004 撤销工单与报修图片 ｜ FP2-5 ｜ P1 ｜ 执行:＿＿ ｜ 结果:＿＿
**场景**: 报修图片可上传引用；撤销受状态机约束。
**接口链**: `POST /repair-tickets/{id}/attachments` `{files}` → `POST /repair-tickets/{id}/cancel`
**前置与测试数据**: 一条待派单工单；一张测试图片。撤销后置"已撤销"不再派单侧由李昂协办确认。
**通过判定**:
① 图片走 SVC-FILE-01，`GET /internal/files/{ref}` 可访问（文件侧兰皓衍）；
② **待派单可撤、已派单/处理中不可撤**（业务错误）；
③ 撤销后工单置"已撤销"不再派单。

### 4.9 C9 巡检 / 保洁 / 报表（P2 · 你×徐亦尘）— 你主执行卫生/晚归业务侧

> 数据样本需你按 Apifox 契约补；**IT-C9-002 保洁→报表由徐亦尘主跑，你协办保洁任务完成侧**（见第二节联调协办表）。

#### IT-C9-001 卫生打分→成绩→排名 ｜ FP2-9、FP3-14 ｜ P2 ｜ 执行:＿＿ ｜ 结果:＿＿
**接口链**: `POST /hygiene-records` `{roomId, score}` → `PUT /hygiene-records/{recordId}` `{score}` → `GET /rooms/{roomId}/hygiene` → `GET /hygiene-rankings?yearMonth=`
**通过判定**:
① 成绩与学生可见一致；修改有操作人/时间；
② 排名计算正确、文明寝室入选符合规则；DB: 评分记录一致。

#### IT-C9-003 晚归登记→补充说明 ｜ FP2-7、FP3-13 ｜ P2 ｜ 执行:＿＿ ｜ 结果:＿＿
**接口链**: `POST /late-entries` `{studentId, recordTime}` → `GET /students/{studentId}/late-entries` → `PUT /late-entries/{recordId}/reason` `{reason}`
**通过判定**:
① 登记后学生可见并可在时限内补说明；
② **过期不可补**（业务错误）；DB: 晚归记录与说明关联。

---

## 2. 联调协办（配合他人，非你主跑）

| 用例 | 主执行 | 你的职责 | 你要准备的数据 |
|---|---|---|---|
| IT-C4-001 报修→派单→完成 | 李昂 | 学生端 **STU-08 报修提交 / STU-09 进度查询** | 学生 token、报修请求体 |
| IT-C7-002 文件存储 | 兰皓衍 | 工单图片 **STU-12 上传附件**引用文件 | 一张测试图片 |
| IT-C9-002 保洁→报表 | 徐亦尘 | 保洁任务 **DORM-40 完成** | 一条已存在保洁任务 |

---

## 3. 你的执行认领与闭环

| 链路 | 你主执行 | P0 数 | 协办 | 认领确认 |
|---|---|---|---|---|
| C4 报修 | 1（004 撤单/图片） | 0 | 1（001 学生端） | ⬜ |
| C9 巡检报表 | 2（001 卫生 / 003 晚归） | 0 | 1（002 保洁任务） | ⬜ |
| C4/C7（协办） | 0 | 0 | 2（001 报修学生端 / 002 工单图片） | ⬜ |

**你的闭环清单**：
- [ ] **C4-004、C9-001、C9-003 数据样本按 Apifox 契约补齐**
- [ ] 与李昂（C4-001 学生端）、徐亦尘（C9-002 保洁）、兰皓衍（C7-002 图片）对齐
- [ ] 逐条校核通过判定与实现一致
- [ ] 8/15 波次 1（C6 前的造数无关，你主跑在波次 2/3）+ 波次 3（C9）执行完
- [ ] PR #35 评审后补实现确认（C4/C9 依赖）

**失败处理**：失败记 `失败+缺陷号` → bug 清单（字段：编号 / 复现步骤（引用用例号+数据样本）/ 所属链路与接口 / 认领模块 / 状态 / 优先级）。

---

## 4. 执行前置依赖（谁欠你 / 你欠谁）

| 依赖 | 对应用例 | 责任人 | 说明 |
|---|---|---|---|
| **PR #35（你的骨架）评审后补实现** | C4、C9 | 兰皓衍评审 | 8/13 前合入，否则跑不动 |
| 报修学生端接口 | C4-004、协办 001 | 你 | STU-08~12 |
| 工单图片/文件侧 | C7-002 | 兰皓衍 | SVC-FILE-01 |
| 本地/云端环境可跑 | 全部 | 黄奇 | 8/13 确认 |
| 各角色测试账号可登录 | 全部 | 各执行人 | 用 §0 预留账号 |

---

## 5. 更新日志 / 校核记录

- 2026-08-16：从《集成测试用例-v1》v1.5 拆分，本人主执行 3 条 + 协办 3 条。
- 校核记录（你填）：＿＿＿＿＿＿
