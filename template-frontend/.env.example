# 浏览器请求的 API 基地址。使用相对路径时由 Vite 开发服务器代理。
VITE_API_BASE_URL=/api

# true 时仅 Mock 登录接口（本地联调用），关闭后请求真实后端。
VITE_USE_MOCK=false

# 仅供 Vite 本地开发代理使用，不会暴露给浏览器。
API_PROXY_TARGET=http://localhost:5000

# 仅供 Vite 使用的本地前端开发端口。
DEV_PORT=3000
