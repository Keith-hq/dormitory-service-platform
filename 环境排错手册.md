**环境排错手册**

Oracle 容器 + 后端 + 前端 常见问题速查

适用项目：dormitory-service-platform
更新日期：2026-08-01 | 版本：v1.0

# 一、Oracle 容器相关
## 1.1 docker compose up -d 失败：端口已占用
**报错信息：**
```
Bind for 0.0.0.0:1521 failed: port is already allocated
```
**原因：**
1521 端口被其他 Oracle 实例或旧容器占用。
**解决方法：**
```
# 1. 查看占用端口的进程
netstat -an | grep 1521
# 2. 清理旧容器
docker ps -a | grep oracle
docker stop <容器名>
docker rm <容器名>
# 3. 或修改 docker-compose.yml 端口映射
# ports:
#   - "1522:1521"   # 改为映射到 1522
```
## 1.2 Oracle 容器一直处于 starting 状态
**原因：**
首次启动需要解压数据文件，3~8 分钟属于正常现象。
**解决方法：**
```
# 查看实时日志
docker logs -f dorm-oracle-db
# 看到以下日志代表初始化完成：
# Pluggable database DORMPDB opened read write
```
## 1.3 ORA-01017: invalid username/password
**原因：**
用户名或密码错误。
**解决方法：**
确认 SYSTEM 密码：Dorm@123456
确认 DORM_OPER 密码：Dorm@2026
注意：@ 符号在连接字符串中可能需要转义或加引号
## 1.4 ORA-00942: table or view does not exist
**原因：**
架构师尚未执行 DDL 建表脚本。
**解决方法：**
联系架构师确认表已创建，并在 DBeaver 中执行：
```
SELECT table_name FROM all_tables WHERE owner = 'DORM_OPER';
```
## 1.5 DBeaver 连接失败：TNS listener does not know of service
**原因：**
服务名拼写错误或容器未完全初始化。
**解决方法：**
```
# 1. 检查容器内监听器状态
docker exec dorm-oracle-db lsnrctl status
# 2. 确认 SERVICE_NAME 使用 DORMPDB（不区分大小写）
# 3. 连接参数：localhost:1521 / Service Name: DORMPDB
```

# 二、后端相关
## 2.1 dotnet run 报 ORA-12154: TNS could not resolve
**原因：**
连接字符串中的 Service Name 不正确。
**解决方法：**
```
# 检查 appsettings.Development.json 中连接字符串：
"OracleConnection": "Data Source=localhost:1521/DORMPDB;User Id=DORM_OPER;Password=Dorm@2026;"
```
## 2.2 dotnet run 报错找不到 Oracle EF Core 包
**原因：**
NuGet 包未正确下载。
**解决方法：**
```
cd template-backend
dotnet restore
dotnet add package Oracle.EntityFrameworkCore --version 8.23.50
```
## 2.3 Swagger 页面空白或接口 404
**原因：**
Controller 未正确注册或路由不匹配。
**解决方法：**
1. 确认 Program.cs 中有 builder.Services.AddControllers() 和 app.MapControllers()
2. 检查 Controller 类是否标记了 [ApiController] 和 [Route("api/[controller]")]
3. 浏览器访问 http://localhost:5000/swagger
## 2.4 500 Internal Server Error + EF Core SQL 日志报错
**原因：**
表字段映射与 Oracle 实际字段名不一致。
**解决方法：**
1. 在 DBeaver 中查看表结构
2. 确认 AppDbContext.OnModelCreating 中 HasColumnName 与 Oracle 列名完全一致（Oracle 默认大写）
3. 启用 EF Core 详细日志："Microsoft.EntityFrameworkCore.Database.Command": "Information"

# 三、前端相关
## 3.1 npm run dev 报 EADDRINUSE 端口占用
**原因：**
3000 端口已被其他进程占用。
**解决方法：**
```
# 修改 vite.config.js
server: {
```
port: 3001,  // 改用 3001
}
## 3.2 前端页面请求报 Network Error
**原因：**
后端未启动，或代理配置不正确。
**解决方法：**
1. 确认后端运行在 http://localhost:5000
2. 确认 vite.config.js 中 proxy 配置正确
3. 浏览器 F12 -> Network 查看实际请求 URL
## 3.3 Cannot read properties of undefined
**原因：**
API 返回数据格式与前端预期不一致。
**解决方法：**
1. F12 查看实际响应的 JSON 结构
2. 确认后端返回格式为 { code: 200, message: "...", data: { items: [...], total: N } }
3. 检查 request.js 响应拦截器中的解构逻辑
# 四、Docker 基础命令
| **操作** | **命令** |
|---|---|
| 启动容器 | docker compose up -d（在 ops-local-oracle 目录下） |
| 停止容器 | docker compose down |
| 查看日志 | docker logs -f dorm-oracle-db |
| 进入容器 | docker exec -it dorm-oracle-db bash |
| 重启容器 | docker restart dorm-oracle-db |
| 查看容器状态 | docker ps |
| 删除容器+数据卷 | docker compose down -v |

# 五、快速诊断流程
**遇到问题时按以下顺序排查，逐层定位根因：**

Oracle 容器是否正常运行？
```
docker ps | grep oracle
```
如果看不到 dorm-oracle-db，说明容器没启动，先执行 docker compose up -d。
数据库表是否存在？
```
-- 在 DBeaver 中执行
SELECT * FROM BUILDINGS;
```
如果报 ORA-00942，说明架构师还没建表，联系架构师执行 DDL。
后端接口是否正常？
```
# 浏览器打开
http://localhost:5000/swagger
```
展开 GET /api/building，执行请求，确认能返回数据。
前端代理是否正确？
```
F12 -> Network -> 检查 /api/* 请求
```
确认请求 URL 被代理到 http://localhost:5000，而非直接请求前端端口。

**以上四步全通过 = 全链路环境正常。任一步失败，在该层锁定问题，参考前面对应章节排查。**
