# 模块联调测试记录 — 刘鸿铭 v1（2026-08-18）

> **认领范围**：公告（DORM-41~44/50）、资产与设施（DORM-12~18/45~47）、共享物品宿管端（DORM-48/49）、保洁（DORM-39/40），共 22 个接口
> **分支**：`feature/fix-liuhongming-0818`（4 个修复提交 + 本文档）
> **口径**：本地 Oracle 21c（DORMDB PDB）+ 种子伪数据集 + `dotnet run` 后端 5000 端口，Apifox 契约对齐

---

## 一、环境与数据准备

| 项 | 内容 |
|---|---|
| Oracle 容器 | `dorm-oracle-db`（gvenzl/oracle-xe:21-full） |
| 业务 schema | `DORM_OPER`（本次在 DORMDB PDB 新建） |
| 初始化 | `foundation/001` + `extensions/010~032` + `sp/*.sql` + `seed/00~05/99` 全部执行，**99_validate 断言全部 0 行** |
| 种子数据 | 2 楼栋 / 48 房 / 31 学生 / 5 管理员 / 144 账单 / 9 公告 / 4 保洁任务 / 6 共享物品 |
| 测试账号 | IT_ADMIN_001（宿管）、IT_STU_001（学生），密码 Temp@123 |
| 后端配置 | User Secrets（OracleConnection + Jwt + ServiceKey，未提交仓库） |

> ⚠ 建库过程发现：`extensions/012~032` 中的匿名 PL/SQL 块按 DBeaver 风格编写（`END;` 后无 `/`），
> 用 SQL*Plus 批跑会吞掉后续脚本。本次以 `sed` 在 `END;` 后补 `/` 执行；建议架构师在脚本注释中补充 SQL*Plus 执行方式说明。

## 二、测试结果：31 项用例全绿

| 模块 | 用例数 | 结果 |
|---|---|---|
| 公告：列表/详情/发布(置顶)/修改(取消置顶)/排序复核/删除/删除后404 | 7 | ✅ 全通过 |
| 资产：房间资产列表/登记/修改/盘点/删除(无关联)/登记(损坏)/损坏转报修/损耗预警列表/处理预警/删除(已关联报修被拒 409) | 10 | ✅ 全通过 |
| 设施：列表/详情/新增/停用/恢复启用/删除 | 6 | ✅ 全通过 |
| 共享物品：发布/维护/删除 | 3 | ✅ 全通过 |
| 保洁：任务列表/完成任务/完成幂等复核 | 3 | ✅ 全通过 |
| 权限边界：学生可看公告 200 / 学生发布被拒 403 | 2 | ✅ 全通过 |

**关键业务规则验证**：
- 资产已关联报修工单时删除 → 409 阻断 ✅（跨模块读取 RepairTickets）
- 资产损坏转报修 → 经 RepairRepository 生成报修工单 ✅（跨模块写入）
- 保洁列表 JOIN 设施档案返回 FacilityCode ✅（跨模块读取）
- 保洁完成幂等：重复完成返回成功且状态不变 ✅（IT-C9-002 口径）
- 公告置顶排序：置顶优先 + 发布时间倒序 ✅

## 三、本次联调发现并修复的 Bug（4 个）

| # | 现象 | 根因 | 修复 | 提交 |
|---|---|---|---|---|
| 1 | `GET /api/notices` 500，公告模块全挂 | Notice↔NoticeDisplay 1:1 双向导航，System.Text.Json 检测对象环抛 JsonException | JsonOptions 增加 `ReferenceHandler.IgnoreCycles` | 80b1806 |
| 2 | 发布公告后列表 500 | CreateAsync 未写 AdminId，D_Notice.Admin_ID 落 NULL，EF 读入非空 string 触发 ORA-50032 | 从 JWT ClaimTypes.Name 取当前用户写入 AdminId | c1fae64 |
| 3 | 资产登记/删除/转报修/预警 500、转报修 400 | ① 4 处顶层 AnyAsync 被翻译为 TRUE/FALSE 布尔字面量 → ORA-00904（团队已知整改 8411155 同源，本模块遗漏）；② ExecuteSqlRawAsync 把 CancellationToken 误作 SQL 参数 | ① AnyAsync→`CountAsync(...) > 0`；② 改 `new object[]` 显式参数 + token 重载 | 42e3934 |
| 4 | 共享物品发布/删除 500 | 发布前置 Buildings.AnyAsync、删除前置 ItemLoans.AnyAsync，同上 ORA-00904 | AnyAsync→`CountAsync(...) > 0` | e6196ef |

> 上述 1~4 修复后复测：**31/31 全绿**（详见本记录第二节）。

## 四、遗留事项（待 PM/架构师确认，非本模块阻塞）

| # | 事项 | 责任 | 备注 |
|---|---|---|---|
| R-1 | 公告发布未联动通知中心（INotificationService） | 刘鸿铭×兰皓衍 | 模块对照表要求"新公告走通知中心"，当前实现未调用；是否补入需 PM 裁决（涉及收件人范围口径） |
| R-2 | 公告/资产/设施/保洁前端页面缺失 | 阿曼卓勒（GAP-004） | admin 端仅有 Duty/Home/Repair 三页，本模块页面级走查无法执行 |
| R-3 | 保洁任务"生成"入口未发现 C# Job | 兰皓衍（调度框架） | DORM-39/40 的列表/完成已通；生成逻辑疑在 SP_Auto_Complete，建议调度文档明确 |
| R-4 | 本模块接口的 Apifox 全量契约核对 | 刘鸿铭 | 本次按控制器路由与 DTO 校验实测；建议在 Apifox 团队空间逐条回填"已联调"状态与响应样例 |

## 五、证据清单（截图归档建议）

1. `docker ps` 容器运行截图
2. DBeaver：DORM_OPER 连接 + D_Notice/D_Asset 数据查询
3. Apifox：公告/资产/设施/共享物品/保洁各接口 200 响应截图
4. 后端控制台：联调期间的 EF SQL 日志与 Quartz SP 启动日志
5. GitHub：本分支 4 个修复提交 + PR 页面
6. 数据库：seed 99_validate 断言 0 行输出

---

*生成日期：2026-08-18 ｜ 维护人：刘鸿铭 ｜ 下一版：PR 评审整改后按意见更新*
